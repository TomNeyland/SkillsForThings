# Memory Mood

![From a directory of dated AI-assistant memory notes, use collect.py to stage cumulative raw-memory snapshots, send each staged timepoint to a fresh Sonnet subagent with no prior answers, validate the JSON, rerun invalid timepoints, and use render.py to produce index.html; complete when every staged snapshot is valid and the page opens.](assets/memory-mood.png)
